.container
    %h2 Edit Board
    = form_with(model: @board, local: true) do |f|
        %div
            = f.label :name, 'Name'
            = f.text_field :name
        %div
            = f.label :description, 'Description'
            = f.text_area :description
        .actions
            = f.submit "Submit"